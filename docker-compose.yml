services:
  material-vault:
    image: ghcr.io/biaobiaobiao108/materialvault:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: material-vault
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3000
      - NODE_ENV=production
      # - AUTH_PASSWORD=your_secure_password # 可选：预设主密码启用全站鉴权保护
